import math # import a module which contains functions and datas. It's not loaded by default, you must load it before it used.

pi = math.pi # use dot notation, specify the name of the module.
# <module name>  <function name>
print(pi)
print(math.sin(pi/2))

# number padding
number01 = 144.8757654
print(f'I only wnat 2 demical place: {number01:.2f}')
#jfefeifeeeeeef
# fefjeiujsitie
# fgfggrg